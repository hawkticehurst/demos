{
  "name": "demos",
  "version": "0.0.1",
  "description": "A place for prototpying UX experiments for VS Code.",
  "author": "Hawk Ticehurst",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "deploy": "npm run build && gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
